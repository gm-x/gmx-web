{% block style %}
	<link rel="stylesheet" href="{{ base_url() }}/assets/css/tables.css" />
{% endblock %}

<div>
	<div f-table f-divider>
		{% for provider in socialNetworks %}
		<ul>
			<li class="uk-width-1-2@m uk-width-1-2s uk-visible@s">
				<i class="fab fa-{{ social_get_icon(provider) }} fa-lg"></i>
				{{ social_get_title(provider) }}
			</li>
			<li class="uk-width-1-2@m uk-width-1-2@s uk-visible@s">
				{% if userSocials[provider] is defined %}
					<form method="post">
						{{ csrf_token() }}
						<input type="hidden" name="social" value="{{ userSocials[provider].id }}">
						<input type="submit" class="uk-button uk-button-secondary uk-button-small" value="Disconnect">
					</form>
				{% else %}
					<a
						href="{{ path_for(constants.user.ROUTE_SOCIAL, {'provider': provider}) }}"
						class="uk-button uk-button-primary uk-button-small"
					>
						Connect
					</a>
				{% endif %}
			</li>
		</ul>
		{% endfor %}
	</div>
</div>