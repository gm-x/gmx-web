{% extends 'admin/admin.twig' %}

{% block content %}
<div class="uk-card uk-card-default uk-card-body uk-width-1-1@m">
	<a href="{{ path_for('admin_preferences_index') }}">Preferences</a>
	<a href="{{ path_for('admin_users_list') }}">Users</a>
	<a href="{{ path_for('admin_roles_list') }}">Roles</a>
	<a href="{{ path_for('admin_servers_list') }}">Server</a>
	<a href="{{ path_for('admin_players_list') }}">Players</a>
</div>
{% endblock %}
