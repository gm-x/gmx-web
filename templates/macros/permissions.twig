{% macro permission_checkbox(form, name) %}
    {% import 'macros/forms.twig' as forms %}
    {% set element = form.get(name) %}
	<li class="uk-width-xxs@s" s-title="{{ element.getTitle() }}">
        {{ forms.element_checkbox(element, false) }}
	</li>
{% endmacro %}
